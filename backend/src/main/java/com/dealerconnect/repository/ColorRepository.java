package com.dealerconnect.repository;
import com.dealerconnect.entity.Color;
import org.springframework.data.jpa.repository.JpaRepository;
public interface ColorRepository extends JpaRepository<Color, Long> {}
